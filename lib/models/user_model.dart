import 'package:flutter/material.dart';

class UserModel {
  final String? imageSrc, name, date;
  final Color? color;

  UserModel({
    this.imageSrc,
    this.name,
    this.date,
    this.color,
  });
}